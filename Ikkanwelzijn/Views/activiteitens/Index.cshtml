@model IEnumerable<Ikkanwelzijn.Models.activiteiten>

@{
    
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.activiteitnaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.activiteitdatum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.activiteitplaats)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.clienten.clientvoornaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.clienten.clienttussenvoegsel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.clienten.clientachternaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.organisatie.organisatienaam)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.activiteitnaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.activiteitdatum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.activiteitplaats)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clienten.clientvoornaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clienten.clienttussenvoegsel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clienten.clientachternaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.organisatie.organisatienaam)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.activiteitid }) |
                @Html.ActionLink("Details", "Details", new { id=item.activiteitid }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.activiteitid })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
